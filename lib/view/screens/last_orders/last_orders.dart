import 'package:flutter/material.dart';

class LastOrdersScreen extends StatelessWidget {
  const LastOrdersScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
